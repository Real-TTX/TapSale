@page
@model LoginModel
@inject TabSale.Web.Services.AppText T
@{ ViewData["Title"] = T["Login"]; Layout = "_AuthLayout"; }
<div class="auth-card">
    <img class="brand-mark" src="~/icons/app-icon.svg" alt="TabSale"/><p class="eyebrow">TABSALE</p><h1>@T["Login"]</h1><p class="muted">@T["LoginHelp"]</p>
    <form method="post" class="stack-form">
        <div asp-validation-summary="ModelOnly" class="validation"></div>
        <label asp-for="Input.UserName">@T["UserName"]</label><input asp-for="Input.UserName" autocomplete="username" />
        <label asp-for="Input.Password">@T["Password"]</label><input asp-for="Input.Password" autocomplete="current-password" />
        <button class="btn btn-primary btn-large" type="submit">@T["Login"]</button>
    </form>
</div>
